import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.eldecker.ionic.kfzunterscheidungszeichen',
  appName: 'Kfz-Unterscheidungszeichen',
  webDir: 'www'
};

export default config;
